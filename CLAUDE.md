@AGENTS.md

# Japanese Verb Trainer — 项目约束

个人 JLPT 备考工具，唯一用户是站主本人。一切设计决策以「能不能帮我更快掌握活用规则」为唯一标准。
产品规格见 `docs/SPEC.md`，与本文件冲突时以本文件为准。

## 铁律

1. **活用答案与推理链由确定性引擎计算，绝不由大模型生成。**
   引擎是纯函数，可被单测锁死。大模型只写「文案」：规则讲解、记忆提示、例句、错因的自然语言展开。
   文案错了顶多啰嗦，变形错了就是教错东西。

2. **掌握度粒度必须是具体规则，不是语法点。**
   统计单位是 `く→いて`、`む・ぶ・ぬ→んで` 这种级别，不是「て形」。

3. **判定模式（一段/五段/不规则）是独立技能模块。**
   它有自己的练习入口和自己的掌握度（`skill-classify`），不得混进 ます形、て形等具体知识点的
   学习或练习流程里。做具体规则练习时不再要求用户重新判断动词类型。

4. **只做 N5。** N5 全链路（学习 → 规则应用 → 输出 → 掌握度）跑通并经站主实际使用认可之前，
   不建 N4/N3 的目录、课程条目、数据字段或 level 分支逻辑。

5. **陷阱动词不可省。** 动词分类的练习素材必须覆盖「长得像一段、实际是五段」的词：
   帰る、入る、走る、知る、要る、切る、滑る、照る、握る等。て形的 行く 这类特例本身就是
   要学要练的知识点，不得因为是特例而简化。

6. **错误反馈必须给完整推理链**，不能只给正确答案。
   例：読む → 五段 → む 结尾 → 适用规则「む・ぶ・ぬ→んで」→ 読んで。

7. **不做账号注册、多用户体系、支付。** 访问保护就是单密码 + 签名 Cookie，没有 user 表。

8. **不为规模做设计。** 单用户，不做性能优化、水平扩展、缓存分层这类事。

## 架构约定

- 静态内容（规则表、动词库）的真身是 `src/data/*.ts` 里的 TypeScript 常量，**不入库**。
  数据库只存会变的东西：作答流水、掌握度、LLM 内容缓存。
  因此 `ruleId` / `verbId` 是字符串，不做数据库外键——完整性由 TypeScript 保证。
- `attempts` 表是掌握度的唯一事实来源，只追加不删除。`rule_mastery` 是可从它重算的派生表。
- LLM 生成内容一律经 `generated_content` 缓存，命中直接返回。缓存键含 `promptVersion` 和
  `model`，改 prompt 或换模型自然失效，不需要手动清库。
- API Key 只在服务端读，永不下发浏览器。任何 `NEXT_PUBLIC_` 前缀的密钥都是 bug。
- `proxy.ts` 是第一道闸门，但每个页面/API 仍要自己调 `requireSession()` / `unauthorized()`
  再验一次（Next 官方建议，防 matcher 漏配）。

## 技术栈

Next.js 16 App Router + TypeScript + Tailwind v4 / Neon Postgres + Drizzle / 部署 Vercel。

- Next.js 16 已把 `middleware.ts` 更名为 `proxy.ts`，用后者。
- 不用 `next/font/google`：构建时要连 Google Fonts，国内网络下会让本地 build 失败。
  用 `globals.css` 里的系统字体栈，日文用 `.jp` class。
- 包管理器是 npm（机器上没有 pnpm）。

## 提交前

```bash
npm run typecheck && npm run lint && npm run build
```
